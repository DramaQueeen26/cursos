"use strict";
(() => {
    var _a;
    const batman = 'Batman';
    const greenLatern = 'Linterna Verde';
    const blackVolvano = `Volcan Negro`;
    console.log(`I'm ${greenLatern}`);
    console.log(batman.toUpperCase());
    console.log(((_a = batman[10]) === null || _a === void 0 ? void 0 : _a.toUpperCase()) || 'No está presente');
})();
